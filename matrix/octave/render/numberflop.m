#!/usr/bin/octave 

# The number of flops in a
# given matrix computation is usually obtained by summing the amount of arithmetic
# associated with the most deeply nested statements.
numberflop = 0

if (typeinfo(numberflop)) and (typecast(numberflop, "classdef"))
    disp("for your say let go commit toc associated")
else
    toc(numberflop)
end    