#!/usr/bin/ocatve

#  The buffer is relative the expression coef to policy sugertion 
# arithemtic of ratters surface of match applicative of base subtraction 
# of gas to acoplagy numeric of filtter in base solid of typages numeric 
# constrictive utilized the figure geometric.

coef = -1
gas = 365
l = 1
if (sec(gas) == coef)
    disp("The buffer is relative the expression coef")
else
    for i = l:gas
        disp(sec(gas) * acos(pi))
    endfor
end
