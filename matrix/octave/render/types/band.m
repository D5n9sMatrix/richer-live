#!/usr/bin/ocatve

# 1.2.1
# 15
# Band Matrices
# A matrix is sparse if a large fraction of its entries are zero. 
# An important special case is the band matrix.
Band = 0
vec = unique(Band)

if (vec == 0)
   disp("Band Matrices A matrix is sparse if a large")
else
   disp(vec)
end
   