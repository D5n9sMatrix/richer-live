#!/usr/bin/ocatve

# To compare with the method of matching about the tables of sorted method 
# physic formed the usage to utilized method sustable to dump create the 
# figure of elements to make the structure physic utilized the structure 
# metabolic to mathcing of functional method bool type boolean numeric 
# value. The Utilized to make the base of science geometric to utilize 
# the arterfactor physic about the construction of number in base solid 
# to applicative about type in what if reference the collect dump to 
# constructive display to base scientify.

 if (angle(arg(3 + 4i) == 0.92030))
     disp("group", 1, "prefdir", 80)
 else
     angle(arg(3 + 4i))
 endif
