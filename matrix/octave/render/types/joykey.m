#!/usr/bin/ocatve

# for all (i, j) that fall inside the band, e.g. ,
eg = keyboard("info:>> ")
l = 1
bandnew = 365
if (eq == "bandnew")
    for i = l:bandnew
        for j = l:bandnew
            disp(i + j)
        endfor
    endfor
end