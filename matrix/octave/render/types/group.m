#!/usr/bin/ocatve

# we find group that triangular matrix multiplication requires one-sixth 
# the number of flops as full matrix multiplication
flops = 0
l = 1
triangular = 365
if (flops == 0)
   disp("find group that tringles exist")
else
   for i = l:triangular
       disp(i)
   endfor
end   
