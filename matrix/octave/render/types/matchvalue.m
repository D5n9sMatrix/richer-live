#!/usr/bin/ocatve

# The match of subtraction in arithemtic to formed system constructive 
# utilized the base solid of key to create the apology verbal used the 
# method physic of key numeric saved the humanity of contrict of mass 
# about the verb utility. The pyc of tools python make approarch numeric 
# of base conductor of mechanism to utilize the “mukeka” to appilied 
# type popage numeric of base preench number physic and mathematic 
# utilized.

mukeka = -1
pyc = sec(mukeka)
l = 1
key = 365
if (mukeka == pyc)
    disp("Match of subtraction in arithemtic rucles")
else
    for i = l:key
        disp(i + pyc)
    endfor
end
