#!/usr/bin/ocatve

# where U = YZ. Thus, = XU = XYZ and only O ( n 3 ) operations are required.
# Use this met
U = 2522
XU = input("On:>> ")
On = 3

if (angle(On) == U)
    disp(XU)
else
   disp("Text * pi full cio")
endif
