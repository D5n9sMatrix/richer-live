#!/usr/bin/ocatve

# < Cm x n , then we designate the real and imaginary parts ofA by Re(A) =
# Band lm(A) = C, respectively. The conjugate of A is the matrix A = (aij ) ·
Cm = 27
n = 1
real = 365
A = n
lm = real

if (conj(Cm) == real == lm)
   disp("then we designate the real and imaginary")
else
   for i = n:real
       disp(i * lm)
   endfor
end
