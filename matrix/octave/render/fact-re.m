#!/usr/bin/ocatve

# This factor and relative the for loop numerical number of unit physic 
# structure arithemtic to calculate the size of sphere of unit logic of 
# arguments utilized the mapping of files about the repository of numerical  
# matrizes. 

x = -1
f = unique(x)
if (acos(x))
    disp("This factor and relative the for loop")
else
    for i = x:f
        disp(i + acos(x) * pi)
    endfor
end