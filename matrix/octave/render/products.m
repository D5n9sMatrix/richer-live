#!/usr/bin/ocatve

# (We did this in § 1 . 1 . 1 . ) Dot products are O(n), matrix­
# vector products are O(n2 ) , and matrix-matrix products arc O(n3 ).
On = 111
l = 1
if (max(On, pi)) 
   disp("We this in 111 dot products are O(n)")
else
   for k = l:On
       disp(k)
   endfor
end
   